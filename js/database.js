/* ============================================================
   MAGAMA — database.js
   Base de datos de productos.
   Gestiona tus productos desde el Panel Admin (admin/index.html).
   Este archivo se usa como respaldo cuando no hay datos en localStorage.
   ============================================================ */

const MAGAMA_DB = {
  productos: [
    /* Tus productos se agregan desde el Panel Admin.
       Ve a: admin/index.html → Agregar Producto        */
  ]
};